xpack:
  monitoring:
    enabled: true
    elasticsearch:
      username: "elastic"
      password: "${ELASTICSEARCH_PASSWORD}"
      hosts: ["http://elasticsearch:9200"]
      ssl:
        verification_mode: none